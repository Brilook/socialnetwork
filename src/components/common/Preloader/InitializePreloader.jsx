import React from "react";
import loader from '../../images/goo-loader.gif'

const InitializePreloader = () => {
  return <img width={'100%'} src={loader} alt={'loader'}/>
};

export default InitializePreloader;